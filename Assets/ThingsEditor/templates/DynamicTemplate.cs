using Miniscript;

namespace ThingsEditor.templates
{
    public class DynamicTemplate
    {
        public static void CreateGlobals(Interpreter interpreter)
        {
            var f = Intrinsic.Create("");
            var map = new ValMap();
            // interpreter.SetGlobalValue();
        }
    }
}